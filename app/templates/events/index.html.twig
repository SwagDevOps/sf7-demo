{% extends 'base.html.twig' %}
{% trans_default_domain 'app' %}

{% block content %}
    {%  if events.getTotalItemCount() %}
        {{ include('events/_index/_events.html.twig') }}
    {% else %}
        {{ include('events/_index/_empty.html.twig') }}
    {% endif %}
{% endblock %}
